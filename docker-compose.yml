#
# Base compose shared by local and production.
#

services:
  app:
    container_name: chat-monolith-app
    ports:
      - "8090:8090"
      # Actuator/metrics (management.server.port)
      - "9091:9091"
    env_file:
      - .env
    environment:
      SERVER_ADDRESS: "0.0.0.0"

      SPRING_MAIL_HOST: "mailpit"
      SPRING_MAIL_PORT: "1025"
      SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH: "false"
      SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE: "false"

      # If you want persistence later, uncomment and adjust:
      # SPRING_DATASOURCE_URL: "jdbc:h2:file:/data/testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL"
      # SPRING_JPA_HIBERNATE_DDL_AUTO: "update"
    depends_on:
      minio-init:
        condition: service_completed_successfully
      mailpit:
        condition: service_started
      prometheus:
        condition: service_started
    volumes:
      - app_tmp:/tmp

  minio:
    image: minio/minio:RELEASE.2025-01-20T14-49-07Z
    container_name: chat-monolith-minio
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/ready"]
      interval: 2s
      timeout: 3s
      retries: 30
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: "${MINIO_ACCESS_KEY:-minio}"
      MINIO_ROOT_PASSWORD: "${MINIO_SECRET_KEY:-minio12345}"
    volumes:
      - minio_data:/data

  minio-init:
    image: minio/mc:RELEASE.2025-01-17T23-25-50Z
    container_name: chat-monolith-minio-init
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_URL: "${MINIO_URL:-http://minio:9000}"
      MINIO_ACCESS_KEY: "${MINIO_ACCESS_KEY:-minio}"
      MINIO_SECRET_KEY: "${MINIO_SECRET_KEY:-minio12345}"
      MINIO_BUCKET: "${MINIO_BUCKET:-pictures}"
    entrypoint:
      - /bin/sh
      - -lc
      - |
        mc alias set local "$${MINIO_URL}" "$${MINIO_ACCESS_KEY}" "$${MINIO_SECRET_KEY}"
        mc mb -p "local/$${MINIO_BUCKET}" || true
        mc anonymous set download "local/$${MINIO_BUCKET}" || true

  mailpit:
    image: axllent/mailpit:v1.24
    container_name: chat-monolith-mailpit
    ports:
      - "1025:1025"
      - "8025:8025"

  prometheus:
    image: prom/prometheus:v3.5.0
    container_name: chat-monolith-prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    volumes:
      - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro

  grafana:
    image: grafana/grafana:11.6.0
    container_name: chat-monolith-grafana
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: "${GRAFANA_ADMIN_USER:-admin}"
      GF_SECURITY_ADMIN_PASSWORD: "${GRAFANA_ADMIN_PASSWORD:-admin}"
    depends_on:
      prometheus:
        condition: service_started
    volumes:
      - ./observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./observability/grafana/dashboards:/var/lib/grafana/dashboards:ro

volumes:
  minio_data:
  app_tmp:
  # app_data: